- name: Create jenkins jobs
  hosts: localhost
  roles:
    - jenkins
  vars:
    mjobs:
      - { job_name: "cart", repo_name: "https://github.com/vsoujan/cart.git" }

    sjobs:
      - { job_name: "roboshop-terraform", repo_name: "https://github.com/vsoujan/roboshop-terraform.git" }